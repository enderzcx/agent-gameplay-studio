# --- secrets: never commit these ---
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.mobileprovision
credentials.json
secrets.yaml
secrets.yml

# --- build output ---
tools/gamerec/build/
*.app/
build/
dist/
*.o
*.dSYM/

# --- python ---
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/
.mypy_cache/
.ruff_cache/

# --- media: this repo ships synthetic fixtures only ---
# Backstop, not a guarantee. Do not rely on it to keep a real capture out of a commit.
# The committed examples/*.tsv and *.md are hand-written synthetic data, not captures.
*.mp4
*.mov
*.mkv
*.webm
*.m4a
*.wav
*.mp3
*.flac
*.aac
*.opus
*.ogg
*.srt
*.ass
*.vtt
*.b64
*.jsonl

# --- local working output ---
out/
work/
runs/
*.metrics.json
*.status.json
*.meta.json
*.focus.jsonl
*.log

# --- os / editor noise ---
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# 本地模型/伴生工具的 job 目录：**不进版本库**。
# 这些目录里有 attempts/meta/result，含本机绝对路径与调用细节，
# 属于本地审计记录，不应随仓库公开。文件保留在本地，不做删除。
.gemini-companion/
.grok-companion/
